# Infra-level secrets — NOT read by the app (dashboard/ and worker/ have their
# own .env.local files for that). This is just a safe place to park credentials
# you only need for direct Postgres access (Supabase CLI, psql, TablePlus).

# Supabase → Project Settings → Database → the password you set when creating the project
SUPABASE_DB_PASSWORD=
